## ansible-lint Configuration file ##

skip_list:
  - '208' # File permissions not mentioned (Install latest package versions)
  - '401' # Git checkouts must contain explicit version (Install latest dotfiles repo)
  - '403' # Package installs should not use latest (Install Golang)
